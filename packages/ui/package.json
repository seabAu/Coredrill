{
  "name": "@job-workspace/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Shared accessible product views and components; no SQL or privileged APIs.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false --tsBuildInfoFile .turbo/typecheck.tsbuildinfo",
    "lint": "eslint src --max-warnings 0"
  }
}
