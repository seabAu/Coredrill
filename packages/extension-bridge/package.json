{
  "name": "@coredrill/extension-bridge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Bounded outbox, pairing, transfer, acknowledgement, and compatibility protocol.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@coredrill/capture-core": "workspace:*",
    "@coredrill/contracts": "workspace:*"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false --tsBuildInfoFile .turbo/typecheck.tsbuildinfo",
    "lint": "eslint src --max-warnings 0"
  }
}
