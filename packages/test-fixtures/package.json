{
  "name": "@job-workspace/test-fixtures",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Synthetic/licensed shared test fixtures; never a production dependency.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false --tsBuildInfoFile .turbo/typecheck.tsbuildinfo",
    "lint": "eslint src --max-warnings 0"
  }
}
