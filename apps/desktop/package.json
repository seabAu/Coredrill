{
  "name": "@coredrill/desktop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin Tauri 2 and rusqlite native services boundary.",
  "scripts": {
    "build": "pnpm run build:native-probe",
    "build:bundle:linux": "pnpm --filter @coredrill/web build && tauri build --bundles appimage",
    "build:bundle:macos": "pnpm --filter @coredrill/web build && tauri build --bundles app",
    "build:desktop": "pnpm --filter @coredrill/web build && tauri build --no-bundle",
    "build:installer:windows": "pnpm --filter @coredrill/web build && tauri build --bundles nsis",
    "build:native-probe": "cargo build --manifest-path src-tauri/Cargo.toml --locked --no-default-features --features native-storage-probe --bin coredrill-native-storage-probe",
    "dev": "tauri dev",
    "test:native-core": "cargo test --manifest-path src-tauri/Cargo.toml --locked --no-default-features --lib",
    "test:platform": "cargo test --manifest-path src-tauri/Cargo.toml --locked --all-features --lib tauri_app_data_path_uses_the_configured_identifier_on_this_platform",
    "typecheck": "cargo check --manifest-path src-tauri/Cargo.toml --locked --no-default-features --features native-storage-probe --bin coredrill-native-storage-probe",
    "lint": "cargo fmt --manifest-path src-tauri/Cargo.toml --all --check && cargo clippy --manifest-path src-tauri/Cargo.toml --locked --no-default-features --features native-storage-probe --bin coredrill-native-storage-probe -- -D warnings",
    "lint:desktop": "cargo clippy --manifest-path src-tauri/Cargo.toml --locked --no-default-features --all-targets -- -D warnings && cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-features --lib --bins -- -D warnings"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.3"
  }
}
