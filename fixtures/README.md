# Fixtures

Only synthetic, redacted, or explicitly licensed fixtures may be committed. Every non-synthetic fixture requires provenance, retention basis, and a documented right to store it.

- `recovery/` contains the wholly synthetic, checksummed portable vault used by the cross-adapter clean recovery drill.
