# Extractor fixtures

Lawful synthetic captured-page and API-payload goldens live beside their owning package tests under `packages/extractors/test/fixtures/`.

The current corpus covers Schema.org `JobPosting`, explicit selected text, and conservative generic DOM/Readability extraction. It contains no live website content, credentials, applicant data, or private pages.
