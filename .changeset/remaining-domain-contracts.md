---
"@coredrill/application": minor
"@coredrill/contracts": minor
"@coredrill/domain": minor
"@coredrill/observability": minor
---

Define the remaining provider-neutral domain ports, application command/query/result/error conventions, and integer-versioned content-free local diagnostic event with fail-closed attribute redaction.

Compatibility: this establishes the first public versions of these package APIs and diagnostic event version `1`. Sync remains capability-only and deferred; no provider, document, labor-data, extraction, telemetry, or sync runtime is enabled by this change.
