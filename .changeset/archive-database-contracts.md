---
"@coredrill/contracts": minor
"@coredrill/storage-core": minor
---

Add the version-1 portable-archive manifest/checksum boundary and establish the adapter-neutral database port, narrowed transaction session, immutable parameterized SQL envelopes, and reusable isolated database/repository contract harness.

Compatibility: this establishes portable-archive manifest version `1` and the first shared storage contract. No earlier durable archive or database adapter contract exists; later format versions and adapters must preserve documented import and transaction behavior.
