---
"@coredrill/domain": minor
---

Add the first public domain contract: UUIDv7 entity IDs, explicit temporal types, integer-minor-unit money and rates, safe web URLs, source references, confidence, semantic status categories, custom stages, and guarded status transitions.

Migration: none. No durable database schema, serialized archive, or versioned boundary contract exists yet.
