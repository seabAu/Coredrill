---
"@coredrill/capture-core": minor
"@coredrill/web": minor
---

Add validated manual, pasted text or URL, saved HTML or text, and JSON capture paths that write versioned envelopes through the durable local manual-import inbox without fetching pasted URLs.
